# ecams-ai 검증 대상 묶음
- 생성: 2026. 5. 15. PM 10:18:02
- 대상: 4건 (미검증)

## 🤖 Claude(VSCode)에게 보낼 지시

> 다음 항목들의 AI 답변을 wiki와 비교해서 검증해주세요.
> 각 항목마다 아래 출력 형식으로 답해주세요.

### 검증 절차
1. 각 항목의 **AI 답변** 본문을 읽고 SQL·테이블명·컬럼명·메서드명·코드값을 추출
2. 항목의 **관련 wiki 힌트**에 있는 파일을 `Read` 도구로 직접 확인
3. wiki와 일치하지 않거나 합성된 정보 식별
4. 아래 출력 형식대로 항목당 작성

### 출력 형식 (항목당)
```
## #N 판정
- 정확도: 정확 | 부분오류 | 환각
- 신뢰점수: 0~100

### 문제점 (없으면 "없음")
- [구체적 어디가 wiki와 다른지]

### 추정 원인 (해당하는 것 체크)
- [ ] (A) wiki 누락/불완전
- [ ] (B) contextBuilder 우선순위 — Servlet/Class가 컨텍스트에 빠짐
- [ ] (C) 모델 환각 — 컨텍스트에 있는데도 합성
- [ ] (D) CodeMap 오용 — 한국어 키워드만 듣고 코드값을 SQL로 끼워 넣음
- [ ] (E) 캐시 오염
- [ ] (F) 기타: ___

### 코드 수정 제안 (해당 시)
- 파일: `<경로>`
- 함수/위치: <함수명 또는 라인>
- 수정 내용: <구체적 변경 1~3줄>
```

완료 후 종합 요약(전체 N건 중 환각 X건, 부분오류 Y건, 정확 Z건)도 출력해주세요.
**자동 수정은 하지 마십시오. 사용자가 검토 후 "적용해줘"라고 별도 지시할 때만 코드 변경하십시오.**

---
## #1 2026-05-15T13:13:09.474Z
- 모델: `claude` (fastMode=true, concise=false)
- 레포: moon7733/kjbank_html5
- 관련 wiki 힌트:
  - `wiki/moon7733_kjbank_html5/Pages/JS/ApplyRequest.js.md`
  - `wiki/moon7733_kjbank_html5/Pages/Servlets/_webPage_ecmr_Cmr0200Servlet.md`

### 사용자 질문
운영배포화면에 알럿창중에 "체크아웃취소" 로 시작하는 알럿창이 언제뜨는거야?

### AI 답변
운영배포 화면(ApplyRequest.js)에서 "체크아웃취소"로 시작하는 alert이 언제 뜨는지 확인하겠습니다.## 0. 분석 근거
- ApplyRequest.js 1721~1771 라인에서 "체크아웃취소" 관련 로직 확인

## 1. 요약
**"체크아웃취소" alert이 뜨는 시점**: 운영배포 화면에서 **신청 버튼 클릭 후**, SR+시스템의 이전 신청 이력 중 **체크아웃취소 건이 있는 경우** → 진행 여부 확인 대화상자로 표시됩니다.

## 2. 실행 흐름

```
사용자: 운영배포 화면 "신청" 버튼 클릭
  ↓
JS: cmdReqClick() 함수 실행
  ↓
서버 요청: /webPage/ecmr/Cmr0200Servlet (requestType: chk_SrCheckOutCancel)
  ↓
서버 응답 분석:
  - 응답 == "PASS" → 체크아웃취소 이력 없음 → 바로 realApply() 진행
  - 응답 != "PASS" → 체크아웃취소 이력 있음 → confirm dialog 표시
        ↓
확인 대화상자: "체크아웃취소 된 프로그램이 있습니다. 계속 진행하시겠습니까?"
  ↓
사용자: OK 또는 취소 선택
```

## 3. 로직 상세

**조건 (ApplyRequest.js 1673라인)**:
- **시스템속성의 운영배포체크[27] 값이 1인 경우만 작동**
  ```javascript
  if ( getSelectedVal('cboSys').cm_sysinfo.substr(26,1) == "1" )
  ```

**순서**:
1. **1단계**: SR에 딸린 모든 프로그램이 한 번에 신청되도록 체크 (첫 번째 서버 요청)
2. **2단계**: SR+시스템으로 **체크아웃취소 이력 유무 확인** (두 번째 서버 요청)
   - requestType: `chk_SrCheckOutCancel`
   - 매개변수: 사용자ID, 시스템코드, SR ID
3. **분기**:
   - 서버 응답 = "PASS" → **체크아웃취소 이력 없음** → 바로 `realApply()` 진행
   - 서버 응답 = 다른 값 → **체크아웃취소 이력 있음** → confirm dialog로 사용자 확인 요청 (1771라인)

**확인 대화상자 메시지** (1771라인):
```
"체크아웃취소 된 프로그램이 있습니다. 계속 진행하시겠습니까?\n" + [프로그램명 목록]
```

## 4. 주요 파일 및 로직 위치

| 항목 | 값 |
|------|-----|
| JS 파일 | `WebContent/js/ecams/apply/ApplyRequest.js` |
| 함수 | `cmdReqClick()` (위치: 1660~1790 라인) |
| 서버 요청 | `/webPage/ecmr/Cmr0200Servlet` (requestType: `chk_SrCheckOutCancel`) |
| Alert 메시지 시작 문자 | "체크아웃취소 된 프로그램" |

## 5. 유지보수 참고사항
- **작성 배경** (1670라인 주석): 2022년 10월 27일 추가됨
- **목적**: 운영배포 시 체크아웃취소 된 프로그램이 남아있으면 사용자에게 진행 여부를 명시적으로 확인시키기 위함
- **조건 의존성**: 시스템 설정에서 특정 속성값이 필요함 → 이 값이 없으면 alert이 뜨지 않음

## 6. 추천 추가 질문
- [체크아웃취소 이력은 어느 테이블에 저장되고 어떻게 조회하는가?]
- [realApply() 함수 다음에 또 다른 확인 절차가 있는가?]
- [시스템속성 "운영배포체크[27]" 값은 어디에서 관리되는가?]

---
## #2 2026-05-15T13:14:42.650Z
- 모델: `claude` (fastMode=true, concise=false)
- 레포: moon7733/kjbank_html5
- 관련 wiki 힌트:
  - `wiki/moon7733_kjbank_html5/Pages/JS/CheckOutCnl.js.md`
  - `wiki/moon7733_kjbank_html5/Pages/Servlets/_webPage_ecmr_Cmr0101Servlet.md`

### 사용자 질문
체크아웃취소 이력은 어느 테이블에 저장되고 어떻게 조회하는가?

### AI 답변
선조립 컨텍스트에서 "체크아웃취소" 이력 저장·조회 흐름을 찾겠습니다.

CheckOutCnl.js의 `reqQuestConf()` 함수에서 `/webPage/ecmr/Cmr0101Servlet` (requestType: `request_Check_Out_Cancel`)를 호출하므로, 해당 서블릿 위키를 먼저 읽겠습니다.wiki 파일이 없으므로 Java 소스를 직접 탐색하겠습니다.

---
## #3 2026-05-15T13:15:29.882Z
- 모델: `deepseek` (fastMode=true, concise=false)
- 레포: moon7733/kjbank_html5
- 관련 wiki 힌트:
  - `wiki/moon7733_kjbank_html5/Pages/JS/CheckOutCnl.js.md`
  - `wiki/moon7733_kjbank_html5/Pages/JS/ApplyRequest.js.md`

### 사용자 질문
운영배포화면에 알럿창중에 "체크아웃취소" 로 시작하는 알럿창이 언제뜨는거야?

### AI 답변
## 0. 분석 근거 (사용한 Wiki/Graph 파일 명시)
- **CheckOutCnl.js**: 체크아웃취소 관련 JS 파일
- **ApplyRequest.js**: 운영배포 관련 JS 파일

## 1. 요약 (화면 목적 + 서버측 최종 처리 결과)
운영배포 화면에서 "체크아웃취소" 알럿창은 체크아웃 상태의 파일을 운영배포 신청할 때 체크아웃 취소 여부를 확인하는 용도로 표시됩니다. 서버측에서는 체크아웃 상태를 검증하고, 필요한 경우 체크아웃 취소를 요청합니다.

## 2. 실행 흐름 (JS 버튼/이벤트 → Servlet → Java Class → DB)
1. **운영배포 신청 버튼 클릭**: `ApplyRequest.js`의 `cmdReqClick()` 함수 실행.
2. **체크아웃 상태 검증**: `Cmr0200Servlet`의 `chk_SrCheckOutCancel` requestType 호출.
3. **알럿창 표시**: 체크아웃 상태인 파일이 있으면 "체크아웃취소" 알럿창 표시.
4. **체크아웃 취소 요청**: 사용자 확인 후 `request_Check_Out_Cancel` 요청 발생.

## 3. 로직 상세 (Java Class의 핵심 처리 로직)
- **체크아웃 상태 검증**: `chk_SrCheckOutCancel` 메서드에서 체크아웃 상태인 파일 목록을 조회.
- **알럿창 조건**: 체크아웃 상태인 파일이 1개 이상 있을 경우 알럿창 표시.
- **체크아웃 취소 처리**: 사용자 확인 후 `request_Check_Out_Cancel` 메서드로 체크아웃 취소 요청.

## 4. 주요 파일 및 DB 테이블
- **JS 파일**: `ApplyRequest.js`, `CheckOutCnl.js`
- **Servlet**: `Cmr0200Servlet`, `Cmr0101Servlet`
- **DB 테이블**: 체크아웃 상태 정보를 저장하는 테이블 (Wiki에 명시되지 않음)

## 5. 유지보수 참고사항
- 체크아웃 상태 검증 로직은 운영배포 신청 시 필수적으로 수행됩니다.
- 알럿창은 사용자에게 체크아웃 취소 여부를 확인하는 용도이므로, 무시할 경우 운영배포가 진행되지 않을 수 있습니다.

## 6. 추천 추가 질문
- [관련 심화 질문 1] 체크아웃 상태인 파일을 운영배포 신청할 때 체크아웃 취소하지 않고 진행할 수 있는 방법은?
- [관련 심화 질문 2] 체크아웃 취소 시 해당 파일의 수정 내역은 어떻게 처리되나요?
- [관련 심화 질문 3] 체크아웃 상태 검증 로직을 변경하려면 어떤 파일을 수정해야 하나요?

---
## #4 2026-05-15T13:16:36.162Z
- 모델: `deepseek` (fastMode=true, concise=false)
- 레포: moon7733/kjbank_html5
- 관련 wiki 힌트:
  - `wiki/moon7733_kjbank_html5/Pages/JS/ApplyRequest.js.md`
  - `wiki/moon7733_kjbank_html5/Pages/Classes/Cmr0200.md`
  - `wiki/moon7733_kjbank_html5/Pages/Servlets/_webPage_ecmr_Cmr0200Servlet.md`

### 사용자 질문
운영배포화면에서 getdownfilelist 함수 로직 알려줘

### AI 답변
## 0. 분석 근거
- `ApplyRequest.js` 파일 내 `getdownfilelist` 함수 분석
- `Cmr0200Servlet` Wiki 및 `Cmr0200` Class Wiki 참조

## 1. 요약
`getdownfilelist` 함수는 운영배포 화면에서 **체크아웃된 파일 목록을 조회**하는 기능을 수행합니다. 서버로부터 체크아웃 상태인 파일 목록을 받아 화면에 표시하며, 사용자가 선택한 파일을 운영배포 신청 목록에 추가할 수 있도록 합니다.

## 2. 실행 흐름
1. **JS 호출**: `ApplyRequest.js` 내 `getdownfilelist()` 함수 실행
2. **Servlet 요청**: `/webPage/ecmr/Cmr0200Servlet` 호출 (requestType: `getdownfilelist`)
3. **Java 처리**: `Cmr0200.java`의 `getdownfilelist()` 메서드에서 DB 조회
4. **결과 반환**: 체크아웃된 파일 목록을 JSON 형식으로 반환하여 화면에 표시

## 3. 로직 상세
### `getdownfilelist()` 함수
- **입력 파라미터**: 
  - `syscd`: 시스템 코드
  - `srcd`: SR 코드
  - `progcd`: 프로그램 코드
- **처리 과정**:
  1. 체크아웃 상태(`cmr0020.cr_chkout = 'Y'`)인 파일을 `cmr0020` 테이블에서 조회
  2. 파일 정보와 함께 체크아웃 사용자, 체크아웃 시간 등 추가 정보 포함
  3. 결과를 JSON 배열로 반환
- **출력**: 
  - 파일명, 체크아웃 사용자, 체크아웃 시간 등

## 4. 주요 파일 및 DB 테이블
| 구분 | 이름 | 역할 |
|------|------|------|
| JS | `ApplyRequest.js` | 체크아웃 파일 목록 조회 호출 |
| Servlet | `Cmr0200Servlet.java` | 요청 라우팅 |
| Class | `Cmr0200.java` | 체크아웃 파일 목록 조회 로직 처리 |
| DB 테이블 | `cmr0020` | 프로그램 기본정보 (체크아웃 상태 포함) |

## 5. 유지보수 참고사항
- 체크아웃 상태가 `Y`인 파일만 조회되므로, 체크아웃 상태가 업데이트되지 않은 경우 목록에 표시되지 않을 수 있습니다.
- `syscd`, `srcd`, `progcd` 파라미터가 정확히 전달되지 않으면 빈 목록이 반환될 수 있습니다.

## 6. 추천 추가 질문
- 체크아웃 상태를 업데이트하는 로직은 어떻게 되나요?
- 체크아웃 파일 목록 조회 시 필터링 조건을 추가할 수 있나요?
- 체크아웃 취소 시 어떤 테이블이 영향을 받나요?

---
## 사용 방법
1. VSCode 채팅창에서 다음과 같이 입력:
   ```
   @UPDATE_NOTES/20260515_2218_audit.md 이 파일의 검증 절차대로 각 항목 검증해줘
   ```
2. Claude가 각 항목의 wiki를 읽고 판정·원인·수정 제안을 작성합니다.
3. 마음에 드는 수정 제안이 있으면 "이거 적용해줘" 라고 별도 지시.
